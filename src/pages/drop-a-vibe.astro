---
import BaseLayout from '../components/layout/BaseLayout.astro';
import Breadcrumb from '../components/ui/Breadcrumb.astro';
import { url, img } from '../utils/url';
import { pageSchema } from '../utils/schema';

const pageTitle = 'Contact Khizar Imtiaz (khizooo) — Drop a Vibe | Khizooology';
const pageDescription = 'Contact Khizar Imtiaz (khizooo) to share feedback, propose a collaboration, report a bug, suggest a Toolooo idea, or say hello.';
const seoContext = { site: Astro.site!, base: import.meta.env.BASE_URL };
const jsonLd = pageSchema({
  type: 'ContactPage',
  path: '/drop-a-vibe',
  name: pageTitle,
  description: pageDescription,
  aboutCreator: true,
  breadcrumbs: [
    { name: 'Home', path: '/' },
    { name: 'Drop a Vibe', path: '/drop-a-vibe' },
  ],
}, seoContext);

const vibeModes = [
  { label: 'General Feedback', desc: 'Thoughts on Khizooology, ideas, suggestions' },
  { label: 'Collaboration', desc: 'Art commissions, creative partnerships' },
  { label: 'Bug Report', desc: 'Something broken? Let me know' },
  { label: 'Feature Idea', desc: 'A tool or monster you\'d like to see' },
  { label: 'Just Saying Hi', desc: 'No agenda, just good vibes' },
];

const socialLinks = [
  { label: 'Instagram', handle: '@khizooo_art', href: 'https://www.instagram.com/khizooo_art' },
  { label: 'X / Twitter', handle: '@khizaroo', href: 'https://twitter.com/khizaroo' },
  { label: 'GitHub', handle: 'KhizaroOo', href: 'https://github.com/KhizaroOo' },
  { label: 'LinkedIn', handle: 'khizar-imtiaz', href: 'https://www.linkedin.com/in/khizar-imtiaz' },
  { label: 'WhatsApp', handle: '+92 300 7683396', href: 'https://wa.me/923007683396' },
  { label: 'Telegram', handle: '@KhizarImtiaz', href: 'https://t.me/KhizarImtiaz' },
  { label: 'TikTok', handle: '@khizooo_butt', href: 'https://www.tiktok.com/@khizooo_butt' },
  { label: 'Discord', handle: 'khizooo', href: 'https://discordapp.com/users/567964089989726231' },
];
---

<BaseLayout
  title={pageTitle}
  description={pageDescription}
  accentColor="var(--k-accent)"
  jsonLd={jsonLd}
>
  <div class="k-container" style="padding-top: 2rem;">
    <Breadcrumb crumbs={[{ label: 'Drop a Vibe' }]} accentColor="var(--k-accent)" />
  </div>

  <section class="k-section dav-section">
    <div class="k-container dav-inner">

      <!-- Left: Form -->
      <div class="dav-form-side">
        <span class="k-section-label" style="color: var(--k-accent)">Drop a Vibe</span>
        <h1 class="dav-title">Say Hello.</h1>
        <p class="dav-lead">
          Whether it's feedback, a collab idea, a bug, or just a good-vibe message —
          this is the place. Every message gets read.
        </p>

        <!-- Vibe type selector -->
        <div class="vibe-types">
          {vibeModes.map((v, i) => (
            <label class="vibe-type-label">
              <input type="radio" name="vibetype" value={v.label} class="vibe-radio" checked={i === 0} />
              <span class="vibe-type-card">
                <span class="vt-label">{v.label}</span>
              </span>
            </label>
          ))}
        </div>

        <!-- Form -->
        <form class="dav-form" action="#" method="post" novalidate>
          <div class="form-row">
            <div class="form-group">
              <label for="name">Name</label>
              <input type="text" id="name" name="name" placeholder="Your name or alias" required autocomplete="name" />
            </div>
            <div class="form-group">
              <label for="email">Email</label>
              <input type="email" id="email" name="email" placeholder="your@email.com" required autocomplete="email" />
            </div>
          </div>
          <div class="form-group">
            <label for="subject">Subject</label>
            <input type="text" id="subject" name="subject" placeholder="What's this about?" />
          </div>
          <div class="form-group">
            <label for="message">Your Vibe</label>
            <textarea id="message" name="message" rows="5" placeholder="Type your message here... no formality required." required></textarea>
          </div>
          <div class="form-footer">
            <span class="form-note">No backend yet — form integration coming soon. For now, social links work too.</span>
            <button type="submit" class="dav-submit" disabled>
              Drop it →
              <span class="submit-badge">Coming Soon</span>
            </button>
          </div>
        </form>
      </div>

      <!-- Right: Sidebar -->
      <div class="dav-sidebar">
        <div class="dav-monster-card">
          <img src={img('/images/Monsters/toolooo.png')} alt="toolooo" width="80" height="80" />
          <p>"Every conversation starts with a small vibe. Don't hold back."</p>
        </div>

        <div class="dav-connect">
          <h3>Other Ways to Connect</h3>
          <ul class="connect-list">
            {socialLinks.map(s => (
              <li>
                <div class="connect-icon-box">
                  <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/></svg>
                </div>
                <div>
                  <strong>{s.label}</strong>
                  <a href={s.href} target="_blank" rel="noopener">{s.handle}</a>
                </div>
              </li>
            ))}
          </ul>
        </div>

        <div class="dav-promise">
          <h3>The Promise</h3>
          <ul class="promise-list">
            <li>Every message gets read</li>
            <li>No spam, ever</li>
            <li>Genuine replies only</li>
            <li>Good vibes only</li>
          </ul>
        </div>
      </div>

    </div>
  </section>

</BaseLayout>

<style>
  .dav-section { background: var(--k-bg); }
  .dav-inner {
    display: grid; grid-template-columns: 1fr 340px; gap: 4rem; align-items: flex-start;
  }

  .dav-title {
    font-family: 'Poppins', sans-serif;
    font-size: clamp(2rem, 4vw, 3rem); font-weight: 900; line-height: 1.1;
    color: var(--k-text); margin: 0.5rem 0 1rem;
  }
  .dav-lead {
    font-size: 1rem; color: var(--k-text-muted); line-height: 1.7; margin: 0 0 1.75rem;
  }

  /* Vibe types */
  .vibe-types {
    display: flex; flex-wrap: wrap; gap: 0.5rem; margin-bottom: 1.75rem;
  }
  .vibe-type-label { cursor: pointer; }
  .vibe-radio { position: absolute; opacity: 0; width: 0; height: 0; }
  .vibe-type-card {
    display: inline-flex; align-items: center; gap: 0.375rem;
    padding: 0.375rem 0.75rem;
    background: var(--k-bg-card); border: 1px solid var(--k-border);
    border-radius: 999px; transition: all 0.2s;
    font-size: 0.8rem; font-weight: 600; color: var(--k-text-muted);
  }
  .vibe-radio:checked + .vibe-type-card {
    background: var(--k-accent); border-color: var(--k-accent); color: #fff; font-weight: 700;
  }

  /* Form */
  .dav-form { display: flex; flex-direction: column; gap: 1rem; }
  .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
  .form-group { display: flex; flex-direction: column; gap: 0.375rem; }
  .form-group label {
    font-size: 0.8rem; font-weight: 700; font-family: 'Poppins', sans-serif;
    color: var(--k-text);
  }
  .form-group input,
  .form-group textarea {
    background: var(--k-bg-card);
    border: 1px solid var(--k-border);
    border-radius: 0.625rem;
    padding: 0.75rem 1rem;
    font-size: 0.9rem; font-family: 'Mulish', sans-serif;
    color: var(--k-text); outline: none;
    transition: border-color 0.2s;
    resize: vertical;
  }
  .form-group input:focus,
  .form-group textarea:focus {
    border-color: var(--k-accent);
  }
  .form-group input::placeholder,
  .form-group textarea::placeholder { color: var(--k-text-muted); }

  .form-footer {
    display: flex; align-items: center; justify-content: space-between;
    flex-wrap: wrap; gap: 0.75rem;
    padding-top: 0.5rem;
  }
  .form-note { font-size: 0.75rem; color: var(--k-text-muted); max-width: 320px; }

  .dav-submit {
    display: inline-flex; align-items: center; gap: 0.5rem;
    padding: 0.65rem 1.5rem;
    background: var(--k-dark); color: var(--k-bg);
    border: none; border-radius: 0.625rem;
    font-family: 'Poppins', sans-serif; font-weight: 700; font-size: 0.875rem;
    cursor: not-allowed; opacity: 0.7;
    white-space: nowrap;
  }
  .submit-badge {
    display: inline-flex; padding: 0.15rem 0.5rem;
    background: var(--k-accent); color: #fff;
    border-radius: 999px; font-size: 0.65rem; font-weight: 700;
    font-family: 'Poppins', sans-serif; text-transform: uppercase;
  }

  /* SIDEBAR */
  .dav-sidebar {
    position: sticky; top: 5rem;
    display: flex; flex-direction: column; gap: 1.25rem;
  }

  .dav-monster-card {
    background: var(--k-bg-card); border: 1px solid var(--k-border);
    border-left: 4px solid var(--k-accent);
    border-radius: 0 1rem 1rem 0;
    padding: 1.25rem; display: flex; align-items: center; gap: 1rem;
  }
  .dav-monster-card img { flex-shrink: 0; }
  .dav-monster-card p { font-size: 0.85rem; font-style: italic; color: var(--k-text-muted); margin: 0; line-height: 1.5; }

  .dav-connect,
  .dav-promise {
    background: var(--k-bg-card); border: 1px solid var(--k-border);
    border-radius: 1rem; padding: 1.25rem;
  }
  .dav-connect h3, .dav-promise h3 {
    font-family: 'Poppins', sans-serif; font-size: 0.8rem; font-weight: 700;
    text-transform: uppercase; letter-spacing: 0.08em;
    color: var(--k-text-muted); margin: 0 0 0.875rem;
  }

  .connect-list { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 0.75rem; }
  .connect-list li { display: flex; align-items: center; gap: 0.75rem; }
  .connect-icon-box {
    width: 1.875rem; height: 1.875rem; flex-shrink: 0;
    display: flex; align-items: center; justify-content: center;
    border: 1px solid var(--k-border); border-radius: 0.5rem;
    color: var(--k-text-muted);
  }
  .connect-list div { display: flex; flex-direction: column; gap: 0.1rem; }
  .connect-list strong { font-size: 0.75rem; color: var(--k-text); font-family: 'Poppins', sans-serif; font-weight: 700; }
  .connect-list a { font-size: 0.75rem; color: var(--k-accent); text-decoration: none; }
  .connect-list a:hover { text-decoration: underline; }

  .promise-list {
    list-style: none; margin: 0; padding: 0;
    display: flex; flex-direction: column; gap: 0.4rem;
  }
  .promise-list li {
    font-size: 0.85rem; color: var(--k-text-muted);
    display: flex; align-items: center; gap: 0.5rem;
  }
  .promise-list li::before {
    content: '✓'; color: var(--k-accent); font-weight: 700; font-size: 0.75rem;
    flex-shrink: 0;
  }

  @media (max-width: 900px) {
    .dav-inner { grid-template-columns: 1fr; }
    .dav-sidebar { position: static; }
    .form-row { grid-template-columns: 1fr; }
  }
</style>
